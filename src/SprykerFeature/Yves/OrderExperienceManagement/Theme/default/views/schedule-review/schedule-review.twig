{% extends template('page-layout-customer', 'CustomerPage') %}

{% define data = {
    review: _view.review,
    schedule: _view.schedule,
    reviewReasonLabelMap: _view.reviewReasonLabelMap,
    reviewReasonBadgeMap: _view.reviewReasonBadgeMap,
    priceChangeReviewReasons: _view.priceChangeReviewReasons,
    itemFlagLabelMap: _view.itemFlagLabelMap,
    itemFlagBadgeMap: _view.itemFlagBadgeMap,
    shipmentAddressChoices: _view.shipmentAddressChoices,
    approveForm: _view.approveForm,
    scopeChoices: _view.scopeChoices,
    title: ('recurring_orders.review.title' | trans({'%name%': _view.schedule.name})),
    activePage: 'recurring-orders',
} %}

{% block title %}{% endblock %}

{% block customerContent %}
    {% include molecule('schedule-review', 'OrderExperienceManagement') with {
        data: {
            review: data.review,
            schedule: data.schedule,
            reviewReasonLabelMap: data.reviewReasonLabelMap,
            reviewReasonBadgeMap: data.reviewReasonBadgeMap,
            priceChangeReviewReasons: data.priceChangeReviewReasons,
            itemFlagLabelMap: data.itemFlagLabelMap,
            itemFlagBadgeMap: data.itemFlagBadgeMap,
            shipmentAddressChoices: data.shipmentAddressChoices,
            approveForm: data.approveForm,
            scopeChoices: data.scopeChoices,
            title: data.title,
            activePage: data.activePage,
        },
    } only %}
{% endblock %}
