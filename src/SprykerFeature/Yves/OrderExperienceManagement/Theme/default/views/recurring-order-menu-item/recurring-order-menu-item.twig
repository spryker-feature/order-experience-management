{% extends template('widget') %}

{% define data = {
    isActivePage: _widget.isActivePage,
} %}

{% block body %}
    {% include molecule('recurring-order-menu-item', 'OrderExperienceManagement') with {
        data: {
            isActivePage: data.isActivePage,
        }
    } only %}
{% endblock %}
